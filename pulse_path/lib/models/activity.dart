class Activity {
  final String name;
  final double duration; // in hours
  final double distance; // in kilometers
  final int steps;
  final int heartRate; // in BPM

  Activity(this.name, this.duration, this.distance, this.steps, this.heartRate);
}
